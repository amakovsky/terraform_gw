test:
  script:
    - echo 'init'
    - docker run -d -v $(pwd):/app/ -w /app/ hashicorp/terraform:light init
    - echo 'fmt'
    - docker run -d -v $(pwd):/app/ -w /app/ hashicorp/terraform:light fmt -check=true
    - echo 'validate'
    - docker run -d -v $(pwd):/app/ -w /app/ hashicorp/terraform:light validate